/* 
        Estructura del forEach.

    valiable.forEach((item) => {
        codigo;
    })
*/
let hobbies = ['Escuchar musica', 'Jugar Futbol', 'Estudiar'];

hobbies.forEach((item) => {
    console.log(item); // Hara lo mismo que el loop for, solo que lo simplifica. Pero para iterar elementos que se encuentran en una lista.
})

//Resultado:

//Escuchar musica
//Jugar Futbol
//Estudiar
